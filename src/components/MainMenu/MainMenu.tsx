import React from 'react'
import Menu from '@/shared/Menu/Menu'

const MainMenu = () => {
	return (
		<Menu
			items={[
				{
					name: '',
					path: ''
				}
			]}
		/>
	)
}

export default MainMenu
